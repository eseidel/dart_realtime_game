name: shimmer2_shared
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.17.6 <3.0.0"

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
  build_runner: ^2.2.0
  json_serializable: ^6.3.1

dependencies:
  json_annotation: ^4.6.0
  meta: ^1.7.0
  vector_math: ^2.1.2
